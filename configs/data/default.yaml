# configs/data/default.yaml

# 훈련 및 검증 데이터 파일의 경로
train_path: "./data/processed/train.csv"
val_path: "./data/processed/val.csv"

# 훈련 시 사용할 배치 사이즈
batch_size: 8 # 메모리 부족(OOM) 에러 방지를 위해 8로 유지!

# 데이터 로딩에 사용할 CPU 워커 수
num_workers: 4